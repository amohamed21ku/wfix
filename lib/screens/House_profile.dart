import 'package:flutter/material.dart';


class House_profile extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Container(), // Empty container
    );
  }
}